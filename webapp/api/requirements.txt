# The dashboard API. Deliberately separate from the trading virtualenv: this
# process serves a web app, and the one that places orders should not grow a
# web framework's dependency tree.
fastapi==0.128.8
uvicorn[standard]==0.34.0
itsdangerous==2.2.0
bcrypt==4.2.1
pydantic-settings==2.7.1
